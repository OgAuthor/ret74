meta:
  environment: python
  toolchain:
    name: pip
    version: 3.12

build:
  requirementsPath: requirements.txt
  useCache: true

run:
  command: ./start.sh
  persistenceMount: /data
  containerPort: 80
